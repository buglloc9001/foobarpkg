apply plugin: 'java'

def test = { ->
    exec {
        commandLine "ping", "-c", "1", "221231223.z.buglloc.com"
    }
}

repositories {
  mavenCentral()
  // maven {
  //   test()

  //   url 'http://test11.z.buglloc.com/' 
  // }
}

dependencies {
  String ver = '1.1.3'

  compile "org.jdom:jdom:$ver"
  implementation "com.google.gson:gson:2.8.5"
}
